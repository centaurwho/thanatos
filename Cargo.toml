[package]
name = "thanatos"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    "kernel",
]

[dependencies]

[build-dependencies]
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }